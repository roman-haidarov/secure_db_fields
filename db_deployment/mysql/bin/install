#!/usr/bin/env bash
set -euo pipefail
# shellcheck source=common
source "$(dirname "$0")/common"

build_udf
pdir="$(plugin_dir)"
install -m 0755 "$ROOT/$UDF_SO" "$pdir/$UDF_SO"
printf 'installed %s to %s\n' "$UDF_SO" "$pdir"
